# ===== App =====
APP_NAME=满分小说 AI 小说创作平台
DEBUG=false
HOST=0.0.0.0
PORT=8000

# ===== Database =====
# SQLite (dev): sqlite+aiosqlite:///./data/novel_agent.db
# PostgreSQL (prod): postgresql+asyncpg://user:pass@localhost:5432/novel_agent
DATABASE_URL=sqlite+aiosqlite:///./data/novel_agent.db
# Only required for the BUG-002A PostgreSQL read-only preflight.
# Generate a distinct value with at least 32 bytes; do not reuse JWT_SECRET.
JSON_PREFLIGHT_HMAC_KEY=
# BUG-002B maintenance gate. Keep false during normal operation.
# Enable only after every application instance has received the same setting.
LEGACY_JSON_WRITES_FROZEN=false
# Public, non-sensitive incident identifier shown to users and support.
LEGACY_JSON_MAINTENANCE_EVENT_ID=BUG-002B
# Retry hint in seconds (30-3600); this is not a promised completion time.
LEGACY_JSON_MAINTENANCE_RETRY_AFTER=60

# ===== LLM API =====
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# ===== CORS =====
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===== File Upload =====
MAX_UPLOAD_SIZE=10485760

# ===== Rate Limiting =====
RATE_LIMIT_DEFAULT=60/minute
RATE_LIMIT_LLM=10/minute
# Use Redis for multi-process: redis://localhost:6379
RATE_LIMIT_STORAGE_URI=memory://

# ===== Auth / JWT =====
# IMPORTANT: Set this to a secure random string in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
# In DEBUG mode, an insecure default is used if not set.
# In production, the app will refuse to start if not set.
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7

# ===== Sentry (Error Monitoring) =====
# Leave empty to disable. Get your DSN at https://sentry.io/
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
# Set to true to send PII (email, username) to Sentry — disabled by default for privacy
SENTRY_SEND_PII=false

# ===== Logging =====
LOG_LEVEL=INFO

# ===== Environment Selection =====
# Set ENV_FILE to specify which .env file to load
# ENV_FILE=.env.dev for development, ENV_FILE=.env.prod for production
# Default: .env
# ENV_FILE=.env
