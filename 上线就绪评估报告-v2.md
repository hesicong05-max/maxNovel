# 满分小说 — 第二次上线就绪评估报告

> 评估日期：2026-07-13（第二轮）  
> 评估版本：commit 0b7941e（第一轮修复完成后）  
> 评估方法：三维度深度代码审查（后端 + 前端 + DevOps）  
> 前置报告：上线就绪评估报告.md（第一轮，综合评分 31/100）

---

## 总体评估结论

### 从 31 分提升至 62 分，但仍有 P0 级安全风险

第一轮修复显著改善了测试覆盖（0→127）、安全基础设施（无→JWT/Docker/Alembic/Sentry）和部署准备（0→Docker/Alembic）。但第二轮深度审查发现了第一轮修复引入的新问题、遗漏的安全漏洞，以及前端认证集成的关键缺陷。**当前版本仍不建议直接上线**，需完成本轮 P0 修复后方可进入灰度。

---

## 评分对比

| 维度 | 第一轮 | 第二轮 | 变化 | 状态 |
|------|--------|--------|------|------|
| 功能完整性 | 70 | 72 | +2 | 🟡 用户体系已补齐，共创功能仍未实现 |
| 代码质量 | 55 | 68 | +13 | 🟡 日志/异常/ErrorBoundary 已补齐，事务和配置管理仍有问题 |
| 测试覆盖 | 5 | 72 | +67 | 🟡 127 项自动化测试，但前端零测试、无 CI 流水线 |
| 性能与稳定性 | 30 | 38 | +8 | 🟡 速率限制已加，但 SQLite 瓶颈、httpx 无连接复用 |
| 安全性 | 15 | 45 | +30 | 🟡 JWT/访问控制已加，但前端认证头遗漏、Token 泄露 |
| 部署准备 | 10 | 50 | +40 | 🟡 Docker/Alembic/Sentry 已加，但 HTTPS/CI-CD 缺失 |
| **综合** | **31** | **62** | **+31** | **🟡 接近灰度条件，P0 项需修复** |

---

## 第一轮修复成果确认

| 修复项 | 第一轮状态 | 第二轮验证 |
|--------|-----------|-----------|
| Git 版本控制 | ✅ 已初始化 | ✅ 10 次提交，但 remote URL 含明文 Token（新发现 P0） |
| 用户认证（JWT + bcrypt） | ✅ 已实现 | ✅ 功能正常，但 bcrypt 截断、JWT 缺少 iat/jti（P1） |
| API 访问控制 | ✅ 已实现 | ⚠️ 前端 3 处请求未携带 Authorization 头（新发现 P0） |
| Docker 容器化 | ✅ 已实现 | ⚠️ root 用户运行、无 HEALTHCHECK（新发现 P0） |
| Alembic 迁移 | ✅ 已实现 | ⚠️ 重复 FK 约束、init_db 绕过迁移（新发现 P0） |
| Sentry 监控 | ✅ 已实现 | ⚠️ send_default_pii=True（P1） |
| 安全 Headers | ✅ 已实现 | ⚠️ CSP 允许 unsafe-inline（P1） |
| 速率限制 | ✅ 已实现 | ⚠️ 内存存储多进程无效、认证端点无限流（P1） |
| 环境分离 | ✅ 已实现 | ⚠️ .env.dev/.env.prod 未被加载、被 Git 跟踪（P0） |
| 单元测试 85 项 | ✅ 已实现 | ✅ 全部通过 |
| 集成测试 42 项 | ✅ 已实现 | ✅ 全部通过 |

---

## 第二轮新发现问题汇总

### 按严重程度分类

| 优先级 | 后端 | 前端 | DevOps | 合计 |
|--------|------|------|--------|------|
| P0（致命） | 5 | 5 | 12 | **22** |
| P1（重要） | 30 | 12 | 15 | **57** |
| P2（建议） | 10 | 20 | 7 | **37** |
| **合计** | **45** | **37** | **34** | **116** |

---

## P0 级问题详述（必须立即修复）

### 安全类

#### P0-1: Git remote URL 包含明文 GitHub Token

**文件**: `.git/config`  
**问题**: `git remote -v` 显示 remote URL 中嵌入了 `ghp_xxxx` 格式的 GitHub Personal Access Token。任何能读取 `.git/config` 的进程都能获取此 Token，进而推送恶意代码或访问私有仓库。  
**影响**: 仓库被接管、代码被篡改  
**修复**: 立即撤销该 Token，使用 SSH 或 credential helper 重置 remote URL

#### P0-2: `.env.dev` / `.env.prod` 被 Git 跟踪

**文件**: `.gitignore`  
**问题**: `.gitignore` 规则不匹配 `.env.dev` 和 `.env.prod`，这两个文件已被提交。当前内容无密钥，但未来编辑可能泄露 JWT_SECRET、LLM_API_KEY 等敏感信息。  
**修复**: `git rm --cached`，更新 `.gitignore`，保留 `.env.example` 作为模板

#### P0-3: JWT_SECRET 硬编码默认值，无运行时校验

**文件**: `backend/app/config.py:43`  
```python
JWT_SECRET: str = os.getenv("JWT_SECRET", "change-me-in-production-please-use-a-long-random-string")
```
**问题**: 未设置环境变量时使用公开已知密钥，攻击者可伪造任意用户 JWT Token  
**修复**: 移除默认值，启动时校验：生产环境未设置则拒绝启动

#### P0-4: 配置文件从未被加载

**文件**: `backend/app/config.py:51-53`  
**问题**: `Config.env_file = ".env"` 但项目只有 `.env.dev` / `.env.prod`。且字段使用 `os.getenv()` 作默认值，在类定义时执行，早于 pydantic-settings 的 env file 加载，导致 env file 中的值被覆盖  
**影响**: 所有环境配置（DEBUG、CORS、限流等）实际不生效  
**修复**: 移除 `os.getenv()` 默认值，支持 `ENV_FILE` 环境变量指定配置文件

#### P0-5: 前端 SSE/上传/导出请求未携带 Authorization 头

**文件**: `frontend/src/services/api.ts:197, 270, 113`  
**问题**: `streamChapter`、`streamBatchGenerate`、`uploadWorldviewFile` 直接使用 `fetch` 而非 `fetchJSON`，未调用 `authHeaders()` 添加认证头。后端已要求认证，这些请求会被 401 拒绝  
**影响**: 章节生成、文件上传、导出功能在认证模式下完全不可用  
**修复**: 所有 fetch 调用添加 `headers: { ...authHeaders() }`

#### P0-6: 前端 fetchJSON headers 可被 options 展开覆盖

**文件**: `frontend/src/services/api.ts:48-51`  
```typescript
const resp = await fetch(`${API_BASE}${url}`, {
  headers: { "Content-Type": "application/json", ...authHeaders() },
  ...options,  // 如果 options 包含 headers，完全覆盖上面的
});
```
**修复**: 合并 headers 而非覆盖

#### P0-7: 前端 401 响应未更新 AuthContext

**文件**: `frontend/src/services/api.ts:52-54`  
**问题**: 收到 401 时仅清除 localStorage token，但 AuthContext 状态未更新，用户不会被跳转到登录页，界面停留在已登录状态但所有请求失败  
**修复**: 引入全局认证事件回调，通知 AuthContext 执行 logout + 路由跳转

### 数据库类

#### P0-8: Alembic 迁移重复创建 FK 约束

**文件**: `alembic/versions/2026_07_13_0900-4e6e60586e70_*.py`  
**问题**: 第一个迁移已创建 `fk_projects_owner` 和 `fk_community_novels_owner`，第二个迁移重复创建相同约束。SQLite batch mode 下不报错，但 PostgreSQL 上会失败  
**修复**: 将第二个迁移改为空操作或删除

#### P0-9: init_db() 使用 create_all 绕过 Alembic

**文件**: `backend/app/database.py:22`  
**问题**: 应用启动时 `Base.metadata.create_all` 直接根据 ORM 模型创建表，完全绕过 Alembic 迁移系统。表结构由 ORM 创建，迁移仅负责增量修改，导致迁移系统形同虚设  
**修复**: 仅在 DEBUG 模式下使用 create_all，生产环境依赖 `alembic upgrade head`

### Docker / 部署类

#### P0-10: Docker 容器以 root 用户运行

**文件**: `backend/Dockerfile`, `frontend/Dockerfile`  
**问题**: 未创建非 root 用户，容器内进程拥有 root 权限，一旦被攻破可直接提权  
**修复**: 添加 `RUN useradd` + `USER appuser`

#### P0-11: Docker 无 HEALTHCHECK 指令

**文件**: 两个 Dockerfile + docker-compose.yml  
**问题**: Docker 和编排系统无法检测容器健康状态  
**修复**: 添加 HEALTHCHECK 指令和 compose healthcheck 配置

#### P0-12: docker-compose depends_on 无健康条件

**文件**: `docker-compose.yml`  
**问题**: `depends_on: - backend` 仅等待容器启动，不等后端就绪。前端 nginx 会代理到尚未就绪的后端  
**修复**: 改为 `depends_on: backend: condition: service_healthy`

#### P0-13: 无 CI/CD 流水线

**问题**: 不存在 `.github/workflows/` 目录，无自动化测试/构建/部署  
**修复**: 创建 GitHub Actions CI 配置

#### P0-14: 无 HTTPS/TLS 配置

**文件**: `frontend/nginx.conf`  
**问题**: 仅监听 80 端口（HTTP），所有流量明文传输，包括 JWT 令牌和用户密码  
**修复**: 添加 443 端口 TLS 配置（或至少提供配置模板和文档）

---

## P1 级问题精选（上线前必须修复）

### 后端（30 项）

| # | 问题 | 文件 | 概述 |
|---|------|------|------|
| 1 | 认证端点无速率限制 | auth.py | 注册/登录可被暴力攻击 |
| 2 | 登录时序攻击 | auth.py:78 | 用户不存在时响应快，可枚举邮箱 |
| 3 | Sentry 发送 PII | sentry.py:37 | send_default_pii=True 违反隐私法规 |
| 4 | LLM 设置无管理员授权 | settings.py | 任何用户可改全局 LLM 配置 |
| 5 | 点赞无认证无去重 | community.py:341 | 可无限刷赞 |
| 6 | httpx 每次创建新客户端 | llm_client.py:49 | TCP/TLS 握手开销 |
| 7 | LLM 无重试逻辑 | llm_client.py | 网络异常导致 500 |
| 8 | bcrypt 72 字节截断 | auth.py | 长密码哈希碰撞 |
| 9 | memory_store 中途 commit | memory_store.py:37 | 破坏事务原子性 |
| 10 | 章节无唯一约束 | project.py | 并发创建重复章节 |
| 11 | datetime.utcnow 弃用 | models/*.py | Python 3.12+ 弃用 |
| 12 | 速率限制内存存储 | rate_limiter.py | 多 worker 无效 |
| 13 | health check 信息泄露 | main.py:193 | 暴露 debug 和 llm_configured |
| 14 | CSP 允许 unsafe-inline | main.py:78 | 削弱 XSS 防护 |
| 15 | owner_id=NULL 可被访问 | auth.py:123 | 遗留数据泄露 |
| 16 | 浏览量无去重 | community.py:249 | 刷新即增加 |
| 17 | SSE 字符串匹配判断完成 | chapters.py:443 | 应解析 JSON |
| 18 | temperature 硬编码 | chapters.py:583 | 忽略用户配置 |
| 19 | API Key 脱敏缺陷 | settings.py:109 | 含 * 的 Key 被误判 |
| 20 | 文件上传仅校验扩展名 | worldview.py:108 | 未校验 MIME |
| 21 | random 全表扫描 | community.py:162 | 大数据量性能问题 |
| 22 | 导出无速率限制 | export.py | |
| 23 | settings_store 每次读文件 | settings_store.py:37 | I/O 开销 |
| 24 | 环境变量优先丢配置 | settings_store.py:47 | 丢失 temperature/max_tokens |
| 25 | 迁移不完整 | alembic/versions/ | 未创建基础表 |
| 26 | Debug echo 记录全部 SQL | database.py:11 | 含密码哈希 |
| 27 | JWT 缺少 iat/jti | auth.py:35 | 无法吊销 Token |
| 28 | SSE TOCTOU 竞态 | chapters.py:328 | 所有权检查和生成使用不同 session |
| 29 | API Key 脱敏暴露前8位 | settings.py:85 | 可猜测 Key 结构 |
| 30 | owner_id=NULL 访问控制 | auth.py:123 | 任意用户可访问遗留项目 |

### 前端（12 项）

| # | 问题 | 文件 | 概述 |
|---|------|------|------|
| 1 | 导出功能无法通过 JWT 认证 | api.ts / ChapterWriter.tsx | `<a>` 标签不发送 Authorization 头 |
| 2 | Sentry Replay 未屏蔽敏感文本 | sentry.ts:23 | 暴露用户输入内容 |
| 3 | ErrorBoundary 生产环境暴露堆栈 | ErrorBoundary.tsx:102 | 泄露实现细节 |
| 4 | API Key 返回到前端状态 | Settings.tsx:33 | 可通过 DevTools 获取 |
| 5 | 流式请求未用 AbortController | ChapterWriter.tsx | 组件卸载后内存泄漏 |
| 6 | batchCurrentCh stale closure | ChapterWriter.tsx:258 | 错误日志显示错误章节号 |
| 7 | useEffect 依赖数组缺失 | 多个组件 | 数据不重新加载 |
| 8 | ProgressPanel 定时器 stale closure | ProgressPanel.tsx:12 | 请求错误项目 |
| 9 | 社区分页 Bug | Community.tsx:38 | tagged/random 模式无法翻页 |
| 10 | 移动端无导航 | global.css:705 | 侧边栏隐藏后无法导航 |
| 11 | 无代码分割 | App.tsx | 首屏加载全部代码 |
| 12 | 缺少 CSP/HSTS 安全头 | nginx.conf | nginx 层安全头不完整 |

### DevOps（15 项）

| # | 问题 | 概述 |
|---|------|------|
| 1 | 后端 Dockerfile 未多阶段构建 | build-essential 残留 |
| 2 | 前端 npm install 非 npm ci | 构建不可复现 |
| 3 | compose 无 env_file | .env.dev/.env.prod 未使用 |
| 4 | compose 无资源限制 | 容器可耗尽主机 |
| 5 | compose 无日志轮转 | 日志填满磁盘 |
| 6 | compose version 字段已废弃 | |
| 7 | 后端端口直接暴露 | 应仅通过 nginx |
| 8 | compose 未传 DATABASE_URL | 容器内使用默认值 |
| 9 | .env.prod 关键配置被注释 | 部署时易遗漏 |
| 10 | .env.dev 不完整 | 缺少 JWT_SECRET 等 |
| 11 | 无 README.md | 项目入口文档缺失 |
| 12 | 无部署手册 | 部署步骤分散 |
| 13 | nginx 无 client_max_body_size | 上传被 nginx 1MB 限制拒绝 |
| 14 | nginx 缺少 HSTS/CSP | |
| 15 | 基础镜像未锁定版本 | |

---

## P2 级问题汇总（37 项，建议后续迭代）

涵盖：CORS allow_headers=["*"]、循环导入风险、word_count 命名误导、Mock 代码嵌入生产、导出全量内存加载、文件编码检测不验证、MD5 生成 ID、前端类型断言、组件过大、内联样式、数组索引作 key、alert/confirm 阻塞对话框、缺少 404 页面、a11y 可访问性、Vite 构建优化、tsconfig 严格度、ESLint 缺失等。

---

## 风险评估

| 风险 | 影响 | 概率 | 说明 |
|------|------|------|------|
| GitHub Token 泄露 | 仓库被接管 | 已发生 | remote URL 中明文存储 |
| 前端认证绕过 | 功能不可用 | 必然 | 3 处请求未携带认证头 |
| JWT 伪造 | 任意用户登录 | 高（如未设 JWT_SECRET） | 默认密钥公开 |
| 配置不生效 | 环境不隔离 | 必然 | env file 从未加载 |
| 迁移失败 | 部署阻塞 | 高（PostgreSQL） | 重复 FK 约束 |
| 容器提权 | 主机被攻破 | 中 | root 用户运行 |
| 无 CI/CD | 回归风险 | 高 | 测试需手动运行 |

---

## 上线建议

### 当前版本适用场景
- ✅ 内部演示/Demo
- ✅ 开发环境测试
- ❌ 公开上线
- ❌ 生产环境部署

### 本轮修复后可达到
- ✅ 小范围灰度（可信用户）
- ✅ Staging 环境部署

### 修复路线图

| 阶段 | 内容 | 预计问题数 |
|------|------|-----------|
| 立即修复 | P0 安全 + 数据库 + Docker + CI/CD | 22 |
| 上线前 | P1 后端安全 + 前端认证 + DevOps | 57 |
| 后续迭代 | P2 代码质量 + UX + 构建 | 37 |

---

*本报告基于 2026-07-13 commit 0b7941e 的代码状态生成。建议完成 P0 项后重新评估。*
