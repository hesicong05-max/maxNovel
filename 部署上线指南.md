# 满分小说 — 生产环境部署上线指南

> **版本**：v6（2026-07-14）  
> **前置条件**：v5 评估通过（85/100），所有 P0/P1 缺陷已修复  
> **综合评分**：92/100（修复后）

---

## 目录

1. [部署前最终检查清单](#1-部署前最终检查清单)
2. [服务器环境准备](#2-服务器环境准备)
3. [项目配置](#3-项目配置)
4. [构建打包流程](#4-构建打包流程)
5. [Docker Compose 部署](#5-docker-compose-部署)
6. [域名与 DNS 设置](#6-域名与-dns-设置)
7. [SSL 证书配置](#7-ssl-证书配置)
8. [Nginx 反向代理配置](#8-nginx-反向代理配置)
9. [CI/CD 流水线配置](#9-cicd-流水线配置)
10. [上线后监控](#10-上线后监控)
11. [回滚方案](#11-回滚方案)
12. [常见问题排查](#12-常见问题排查)

---

## 1. 部署前最终检查清单

### 1.1 代码验证（全部 ✅）

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 后端测试 | ✅ 263/263 通过 | 11 个测试文件 |
| 前端测试 | ✅ 37/37 通过 | 4 个测试文件 |
| TypeScript 编译 | ✅ 零错误 | `tsc --noEmit` |
| 模块导入 | ✅ 全部通过 | 45 路由注册 |
| Alembic 迁移 | ✅ 同步 | 无待迁移 |
| Bandit 安全扫描 | ✅ 0 HIGH | 1 MEDIUM（0.0.0.0 绑定） |
| Git 版本控制 | ✅ 已推送 | main 与 origin 同步 |

### 1.2 配置确认清单

部署前必须完成以下配置：

```bash
# 1. 生成 JWT 密钥
python -c "import secrets; print(secrets.token_urlsafe(64))"
# 复制输出 → 填入 .env 文件的 JWT_SECRET=

# 2. 准备 PostgreSQL 凭据
# 设置强密码：DB_USER=novel_user, DB_PASSWORD=<your-strong-password>

# 3. 准备 LLM API Key
# 在前端 /settings 页面配置，或写入 .env.prod 的 LLM_API_KEY=

# 4. 准备 Sentry DSN（可选）
# 在 https://sentry.io 创建项目，获取 DSN

# 5. 准备域名
# 将域名 DNS A 记录指向服务器 IP
```

### 1.3 服务器最低配置

| 资源 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核 | 4 核 |
| 内存 | 2 GB | 4 GB |
| 磁盘 | 20 GB SSD | 40 GB SSD |
| 带宽 | 5 Mbps | 10 Mbps |
| 操作系统 | Ubuntu 22.04 LTS | Ubuntu 24.04 LTS |

---

## 2. 服务器环境准备

### 2.1 安装 Docker 和 Docker Compose

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Docker
curl -fsSL https://get.docker.com | sudo sh

# 将当前用户加入 docker 组
sudo usermod -aG docker $USER

# 重新登录使组变更生效
exit
# 重新 SSH 登录

# 验证
docker --version          # Docker version 27.x+
docker compose version    # Docker Compose version v2.x+
```

### 2.2 配置防火墙

```bash
# 只开放必要端口
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp          # SSH
sudo ufw allow 80/tcp          # HTTP（Let's Encrypt 验证 + 重定向）
sudo ufw allow 443/tcp         # HTTPS
sudo ufw enable
sudo ufw status verbose
```

### 2.3 配置 Swap（内存不足时）

```bash
# 创建 2GB swap 文件
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 持久化
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# 验证
free -h
```

### 2.4 安装辅助工具

```bash
# 安装常用工具
sudo apt install -y htop curl wget git unzip

# 安装 Certbot（用于 SSL 证书）
sudo apt install -y certbot

# 安装 jq（用于 JSON 解析）
sudo apt install -y jq
```

---

## 3. 项目配置

### 3.1 克隆项目

```bash
cd /opt
sudo git clone https://github.com/hesicong05-max/maxNovel.git novel
sudo chown -R $USER:$USER /opt/novel
cd /opt/novel
```

### 3.2 创建环境变量文件

```bash
# 在项目根目录创建 .env 文件
cp .env.example .env
nano .env
```

填入实际值：

```env
# PostgreSQL
DB_USER=novel_user
DB_PASSWORD=<your-strong-password-here>

# JWT Secret（用第 1 步生成的密钥）
JWT_SECRET=<your-jwt-secret-here>

# Sentry（可选）
SENTRY_DSN=
VITE_SENTRY_DSN=
```

### 3.3 配置后端生产环境

```bash
nano backend/.env.prod
```

关键配置项：

```env
# CORS — 改为你的实际域名
CORS_ORIGINS=https://novel.yourdomain.com

# LLM API（可选，也可在前端设置页面配置）
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o

# 日志级别（生产环境用 WARNING）
LOG_LEVEL=WARNING

# Sentry
SENTRY_DSN=
```

### 3.4 验证配置

```bash
# 检查 .env 文件是否包含所有必需变量
grep -E "^(DB_USER|DB_PASSWORD|JWT_SECRET)=" .env

# 确认没有占位符残留
grep -r "your-domain\|change-me\|your-strong" .env backend/.env.prod
# 应该无输出
```

---

## 4. 构建打包流程

### 4.1 本地预构建验证（可选但推荐）

在本地先验证 Docker 构建是否成功：

```bash
# 构建后端镜像
docker build -t novel-backend:test ./backend

# 构建前端镜像
docker build -t novel-frontend:test ./frontend

# 验证镜像大小
docker images | grep novel
```

### 4.2 服务器上构建

```bash
cd /opt/novel

# 构建所有服务
docker compose build

# 预期输出：
# ✔ backend  Built
# ✔ frontend Built
# ✔ db       Pulled (postgres:16.4-alpine3.20)
```

### 4.3 构建优化（可选）

如果构建速度慢，可以配置 Docker BuildKit：

```bash
# 启用 BuildKit
export DOCKER_BUILDKIT=1

# 或在 /etc/docker/daemon.json 中永久启用
sudo nano /etc/docker/daemon.json
# 添加：{"features": {"buildkit": true}}
sudo systemctl restart docker
```

---

## 5. Docker Compose 部署

### 5.1 首次启动

```bash
cd /opt/novel

# 启动所有服务（后台运行）
docker compose up -d

# 查看启动状态
docker compose ps

# 预期输出：
# NAME            STATUS                   PORTS
# novel-db        Up (healthy)             5432/tcp
# novel-backend   Up (healthy)             8000/tcp
# novel-frontend  Up (healthy)             0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp
```

### 5.2 验证服务

```bash
# 检查后端健康
curl http://localhost:8000/api/health
# 预期：{"status":"ok"}

# 检查前端
curl -I http://localhost/
# 预期：HTTP/1.1 200 OK

# 查看后端日志
docker compose logs backend --tail 50

# 查看数据库迁移日志
docker compose logs backend | grep -A5 "migration"
# 应看到 "Running upgrade" 和 "Starting uvicorn server"
```

### 5.3 数据库初始化验证

```bash
# 进入后端容器检查表
docker compose exec backend python -c "
from app.database import engine
from sqlalchemy import inspect
import asyncio

async def check():
    insp = inspect(engine.sync_engine)
    tables = insp.get_table_names()
    print(f'Tables ({len(tables)}):')
    for t in sorted(tables):
        print(f'  {t}')

asyncio.run(check())
"
```

预期输出包含：`users`, `projects`, `worldviews`, `outlines`, `chapters`, `story_memories`, `community_novels`, `community_tags`, `novel_tag_association`, `alembic_version`

---

## 6. 域名与 DNS 设置

### 6.1 DNS 记录配置

在你的域名服务商（如阿里云、Cloudflare、GoDaddy）配置：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | @ | `<服务器IP>` | 600 |
| A | www | `<服务器IP>` | 600 |
| CNAME | api | `novel.yourdomain.com.` | 600 |

### 6.2 验证 DNS 解析

```bash
# 等待 DNS 传播（通常 5-10 分钟）
dig +short novel.yourdomain.com
# 应返回服务器 IP

# 或使用 nslookup
nslookup novel.yourdomain.com
```

### 6.3 配置 CORS

```bash
# 更新后端 .env.prod
nano backend/.env.prod
# 修改 CORS_ORIGINS=https://novel.yourdomain.com

# 重启后端
docker compose restart backend
```

---

## 7. SSL 证书配置

### 7.1 使用 Let's Encrypt 获取免费证书

```bash
# 停止 nginx（释放 80 端口）
docker compose stop frontend

# 获取证书
sudo certbot certonly --standalone -d novel.yourdomain.com -d www.novel.yourdomain.com

# 预期输出：
# - Congratulations! Your certificate and chain have been saved at:
#   /etc/letsencrypt/live/novel.yourdomain.com/fullchain.pem
#   /etc/letsencrypt/live/novel.yourdomain.com/privkey.pem
```

### 7.2 挂载证书到 Docker

```bash
# 创建 TLS 目录并链接证书
sudo mkdir -p /opt/novel/tls
sudo ln -sf /etc/letsencrypt/live/novel.yourdomain.com/fullchain.pem /opt/novel/tls/fullchain.pem
sudo ln -sf /etc/letsencrypt/live/novel.yourdomain.com/privkey.pem /opt/novel/tls/privkey.pem

# 确保权限
sudo chmod -R 755 /opt/novel/tls
```

### 7.3 配置自动续期

```bash
# 测试续期
sudo certbot renew --dry-run

# 添加自动续期 Cron
sudo crontab -e
# 添加（每天凌晨 3 点检查，每 60 天续期）：
0 3 * * * certbot renew --quiet --post-hook "docker compose -f /opt/novel/docker-compose.yml restart frontend"
```

### 7.4 替代方案：使用 Cloudflare SSL

如果使用 Cloudflare CDN：

1. 在 Cloudflare 控制台设置 SSL 模式为 "Full"（不选 "Full (Strict)" 除非有证书）
2. 在 Cloudflare 控制台 → SSL/TLS → Edge Certificates 启用 "Always Use HTTPS"
3. 服务器端 nginx 只需监听 80 端口（Cloudflare 处理 HTTPS）
4. 修改 docker-compose.yml 的 frontend 服务只暴露 80 端口

---

## 8. Nginx 反向代理配置

前端 Docker 镜像已内置 nginx 配置。如果需要自定义：

### 8.1 检查现有 nginx 配置

```bash
# 查看前端 Dockerfile 中的 nginx 配置
cat frontend/Dockerfile
# 确认 COPY nginx.conf 到 /etc/nginx/conf.d/
```

### 8.2 自定义 nginx.conf（如需修改）

```bash
# 创建自定义 nginx 配置
cat > frontend/nginx.conf << 'EOF'
server {
    listen 80;
    server_name _;

    # 前端静态文件
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到后端
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 支持（关键！）
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        chunked_transfer_encoding on;
    }

    # 健康检查
    location /api/health {
        proxy_pass http://backend:8000;
        access_log off;
    }
}
EOF
```

### 8.3 HTTPS nginx 配置（有 SSL 证书时）

```bash
cat > frontend/nginx-ssl.conf << 'EOF'
server {
    listen 80;
    server_name novel.yourdomain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name novel.yourdomain.com;

    ssl_certificate /etc/nginx/tls/fullchain.pem;
    ssl_certificate_key /etc/nginx/tls/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;

    # 安全头
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy strict-origin-when-cross-origin;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # SSE 关键配置
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        chunked_transfer_encoding on;
    }

    location /api/health {
        proxy_pass http://backend:8000;
        access_log off;
    }
}
EOF
```

---

## 9. CI/CD 流水线配置

### 9.1 推送 CI 配置到 GitHub

CI 文件（`.github/workflows/ci.yml`）需要 `workflow` scope 的 GitHub Token 才能推送：

```bash
# 1. 在 GitHub 创建 Personal Access Token（需要 workflow scope）
#    Settings → Developer settings → Personal access tokens → Tokens (classic)
#    勾选 "repo" 和 "workflow" scope

# 2. 使用新 token 推送
git remote set-url origin https://<username>:<new-token>@github.com/hesicong05-max/maxNovel.git
git add .github/workflows/ci.yml
git commit -m "ci: add CI pipeline with backend+frontend tests"
git push origin main

# 3. 推送后恢复原 URL
git remote set-url origin https://github.com/hesicong05-max/maxNovel.git
```

### 9.2 CI 流水线说明

CI 配置包含 3 个 Job：

| Job | 内容 | 触发条件 |
|-----|------|---------|
| `backend-test` | 安装依赖 → 运行 11 个测试文件 → Bandit 安全扫描 | push/PR to main |
| `frontend-test` | npm ci → tsc 检查 → 运行 Vitest → 构建 | push/PR to main |
| `docker-build` | 构建前后端 Docker 镜像 | 前两个 Job 通过后 |

### 9.3 自动部署（可选）

如果需要 push 后自动部署到服务器：

```bash
# 1. 在服务器上创建部署脚本
cat > /opt/novel/deploy.sh << 'DEPLOY'
#!/bin/bash
set -e
cd /opt/novel
git pull origin main
docker compose build
docker compose up -d
docker image prune -f
echo "Deploy complete at $(date)"
DEPLOY
chmod +x /opt/novel/deploy.sh

# 2. 配置 GitHub Webhook
#    GitHub repo → Settings → Webhooks → Add webhook
#    URL: http://your-server:9000/webhook
#    Content type: application/json
#    Events: Just the push event

# 3. 在服务器上运行 webhook 监听器
#    使用 https://github.com/adnanh/webhook
```

---

## 10. 上线后监控

### 10.1 健康检查

```bash
# 创建监控脚本
cat > /opt/novel/monitor.sh << 'MONITOR'
#!/bin/bash
echo "=== $(date) ==="

# 检查容器状态
docker compose ps

# 检查后端健康
HEALTH=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/health)
if [ "$HEALTH" = "200" ]; then
    echo "✅ Backend: healthy"
else
    echo "❌ Backend: HTTP $HEALTH"
    # 发送告警（如通过 Bark/Pushover/钉钉机器人）
    # curl -s "https://api.day.app/<key>/小说后端异常/HTTP-$HEALTH"
fi

# 检查前端
FRONTEND=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/)
if [ "$FRONTEND" = "200" ]; then
    echo "✅ Frontend: healthy"
else
    echo "❌ Frontend: HTTP $FRONTEND"
fi

# 检查磁盘空间
DISK=$(df -h / | awk 'NR==2 {print $5}' | tr -d '%')
if [ "$DISK" -gt 80 ]; then
    echo "⚠️ Disk usage: ${DISK}%"
fi

# 检查内存
MEM=$(free | awk '/Mem:/ {printf "%.0f", $3/$2*100}')
if [ "$MEM" -gt 80 ]; then
    echo "⚠️ Memory usage: ${MEM}%"
fi

echo ""
MONITOR
chmod +x /opt/novel/monitor.sh

# 添加到 Cron（每 5 分钟检查一次）
crontab -e
# 添加：
*/5 * * * * /opt/novel/monitor.sh >> /var/log/novel-monitor.log 2>&1
```

### 10.2 日志管理

```bash
# 查看实时日志
docker compose logs -f backend

# 查看最近 100 行日志
docker compose logs backend --tail 100

# 日志已配置自动轮转（docker-compose.yml 中 max-size: 10m, max-file: 3）
# 查看日志文件
docker inspect novel-backend | jq '.[0].LogPath'
```

### 10.3 数据库备份

```bash
# 创建备份脚本
cat > /opt/novel/backup.sh << 'BACKUP'
#!/bin/bash
set -e
BACKUP_DIR="/opt/novel/backups"
mkdir -p "$BACKUP_DIR"

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/novel_db_$DATE.sql"

# 从 .env 读取凭据
source /opt/novel/.env

# 导出数据库
docker compose exec -T db pg_dump -U "$DB_USER" -d novel_agent > "$BACKUP_FILE"

# 压缩
gzip "$BACKUP_FILE"

# 保留最近 7 天的备份
find "$BACKUP_DIR" -name "novel_db_*.sql.gz" -mtime +7 -delete

echo "Backup created: ${BACKUP_FILE}.gz ($(du -h ${BACKUP_FILE}.gz | awk '{print $1}'))"
BACKUP
chmod +x /opt/novel/backup.sh

# 添加到 Cron（每天凌晨 2 点备份）
crontab -e
# 添加：
0 2 * * * /opt/novel/backup.sh >> /var/log/novel-backup.log 2>&1
```

### 10.4 性能监控

```bash
# 运行负载测试（在本地机器上执行）
cd backend
pip install locust
locust -f load_tests/locustfile.py --host=https://novel.yourdomain.com \
    --headless -u 50 -r 5 -t 120s

# 查看 Docker 资源使用
docker stats

# 预期输出：
# NAME            CPU %    MEM USAGE / LIMIT
# novel-db        2.5%     120MiB / 512MiB
# novel-backend   5.2%     180MiB / 512MiB
# novel-frontend  0.1%     20MiB / 256MiB
```

### 10.5 Sentry 错误监控

1. 在 https://sentry.io 创建项目（选择 FastAPI / React）
2. 获取 DSN
3. 配置到环境变量：

```bash
# .env 文件
SENTRY_DSN=https://<key>@sentry.io/<project>
VITE_SENTRY_DSN=https://<key>@sentry.io/<project>

# 重启服务
docker compose down
docker compose up -d
```

---

## 11. 回滚方案

### 11.1 快速回滚（版本回退）

```bash
# 查看提交历史
git log --oneline -10

# 回退到指定版本
git checkout <commit-hash>

# 重新构建和部署
docker compose build
docker compose up -d

# 验证
curl http://localhost:8000/api/health
```

### 11.2 数据库回滚

```bash
# 查看迁移历史
docker compose exec backend alembic history

# 回退一个版本
docker compose exec backend alembic downgrade -1

# 回退到指定版本
docker compose exec backend alembic downgrade <revision>

# 恢复数据（从备份）
gunzip < /opt/novel/backups/novel_db_20260114_020000.sql.gz | \
    docker compose exec -T db psql -U "$DB_USER" -d novel_agent
```

### 11.3 完整回滚流程

```bash
#!/bin/bash
# rollback.sh — 完整回滚脚本
set -e

cd /opt/novel

# 1. 停止服务
docker compose down

# 2. 回退代码
git checkout $1  # $1 = commit hash

# 3. 恢复数据库（如果有备份）
if [ -f "$2" ]; then
    source .env
    gunzip < "$2" | docker compose exec -T db psql -U "$DB_USER" -d novel_agent
fi

# 4. 重新构建
docker compose build

# 5. 启动
docker compose up -d

# 6. 验证
sleep 10
curl -f http://localhost:8000/api/health || echo "HEALTH CHECK FAILED!"
```

---

## 12. 常见问题排查

### Q1: 后端启动失败 — "JWT_SECRET must be set"

```bash
# 检查 .env 文件
grep JWT_SECRET .env
# 应该有值，不能为空

# 确保 docker-compose 读取了 .env 文件
docker compose config | grep JWT_SECRET
```

### Q2: 数据库连接失败 — "connection refused"

```bash
# 检查 PostgreSQL 容器是否健康
docker compose ps db
# STATUS 应该是 "Up (healthy)"

# 如果不健康，查看日志
docker compose logs db

# 常见原因：密码不匹配
# 检查 .env 中的 DB_PASSWORD 是否与 PostgreSQL 容器中一致
docker compose exec db env | grep POSTGRES_PASSWORD
```

### Q3: SSE 流式生成不工作

```bash
# 检查 nginx 配置是否有 SSE 关键设置
# 必须包含：
# proxy_buffering off;
# proxy_cache off;
# proxy_read_timeout 300s;

# 检查后端 SSE 端点
curl -N http://localhost:8000/api/chapters/test/stream/1
# 应该看到 data: {...} 格式的流式输出
```

### Q4: 前端页面白屏

```bash
# 检查前端容器日志
docker compose logs frontend

# 检查 nginx 是否在运行
docker compose exec frontend nginx -t

# 检查静态文件是否存在
docker compose exec frontend ls /usr/share/nginx/html/
```

### Q5: LLM API 调用失败

```bash
# 在前端 /settings 页面检查 LLM 配置
# 或在后端检查
docker compose exec backend python -c "
from app.core.settings_store import load_settings
s = load_settings()
print(f'API Key: {s.get(\"api_key\", \"\")[:8]}...')
print(f'Base URL: {s.get(\"base_url\", \"\")}')
print(f'Model: {s.get(\"model\", \"\")}')
"

# 测试连接
docker compose exec backend python -c "
import asyncio
from app.core.llm_client import llm_client
result = asyncio.run(llm_client.test_connection())
print(result)
"
```

### Q6: 磁盘空间不足

```bash
# 清理 Docker 无用镜像
docker system prune -a -f

# 清理旧备份
find /opt/novel/backups -name "*.sql.gz" -mtime +7 -delete

# 查看磁盘使用
df -h
```

---

## 附录：完整部署命令速查

```bash
# === 一次性配置 ===
git clone https://github.com/hesicong05-max/maxNovel.git /opt/novel
cd /opt/novel
cp .env.example .env && nano .env          # 填入密钥
nano backend/.env.prod                      # 配置 CORS 和 LLM

# === SSL 证书 ===
sudo certbot certonly --standalone -d novel.yourdomain.com
sudo ln -sf /etc/letsencrypt/live/novel.yourdomain.com/*.pem /opt/novel/tls/

# === 部署 ===
docker compose build
docker compose up -d

# === 验证 ===
curl http://localhost:8000/api/health
curl -I http://localhost/

# === 日常运维 ===
docker compose logs -f backend              # 查看日志
docker compose restart backend              # 重启后端
docker compose down && docker compose up -d  # 完全重启
/opt/novel/backup.sh                          # 手动备份
/opt/novel/monitor.sh                         # 手动健康检查

# === 更新部署 ===
git pull origin main
docker compose build
docker compose up -d
docker image prune -f
```

---

*文档版本：v6 | 最后更新：2026-07-14 | 适用代码版本：commit e4b055b*
