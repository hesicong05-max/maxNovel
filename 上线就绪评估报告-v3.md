# 满分小说 — 第三次上线就绪评估报告

> 评估日期：2026-07-14（第三轮）
> 评估版本：commit 45f3b8d（第二轮 P0/P1 修复完成后）
> 评估方法：三维度深度代码审查（后端 + 前端 + DevOps）+ 全量测试 + 配置验证
> 前置报告：上线就绪评估报告.md（第一轮 31 分）、上线就绪评估报告-v2.md（第二轮 62 分）

---

## 总体评估结论

### 综合评分 64/100 — 不具备直接上线条件

第二轮 P0/P1 修复（commit 45f3b8d）有效解决了 22 项 P0 和 15 项 P1 问题，使项目从"内部 Demo"提升至"接近灰度"。但第三轮深度审查发现了 **8 个新 P0 级阻塞项** 和 **22 个 P1 级重要问题**，其中最关键的包括：初始迁移不创建表（生产部署必定失败）、LLM 设置端点无管理员授权（可窃取 API Key）、全局限流中间件未注册（约 30 个端点无限流）、前端 Docker 仍以 root 运行、CI 流水线缺少环境变量（测试将直接失败）、以及无 HTTPS/TLS 配置。

**当前版本适用场景：内部演示 / 开发测试。不建议公开上线。**

---

## 评分对比

| 维度 | 第一轮 | 第二轮 | 第三轮 | 变化(v2→v3) | 状态 |
|------|--------|--------|--------|-------------|------|
| 功能完整性 | 70 | 72 | **75** | +3 | 🟡 核心功能完整，认证已修复；点赞/浏览量去重缺失 |
| 代码质量 | 55 | 68 | **72** | +4 | 🟡 日志/异常/事务改善；SSE TOCTOU、缺少唯一约束 |
| 测试覆盖 | 5 | 72 | **73** | +1 | 🟡 127 测试通过；CI 未激活、前端零测试 |
| 性能与稳定性 | 30 | 38 | **48** | +10 | 🟡 Redis 限流支持已加；但中间件未注册，default_limits 无效 |
| 安全性 | 15 | 45 | **58** | +13 | 🟡 JWT 校验/认证头/时序攻击已修复；LLM 设置暴露、无 HTTPS |
| 部署准备 | 10 | 50 | **58** | +8 | 🟡 Docker/CI/Sentry 已建；迁移不建表、CI 未推送、无 HTTPS |
| **综合** | **31** | **62** | **64** | **+2** | **🟡 接近灰度，8 项 P0 需修复** |

> 评分说明：v2→v3 综合分仅提升 2 分，因为第二轮修复带来的提升（约 +6 分）被新发现的 P0 问题（约 -4 分）部分抵消。

---

## 第二轮修复验证（10/10 P0 已确认修复）

| v2 P0 问题 | v3 验证结果 |
|-----------|-----------|
| Git remote URL 明文 Token | ✅ 已移除，remote 为干净的 HTTPS URL |
| .env.dev/.env.prod 被 Git 跟踪 | ✅ 已从 Git 移除，.gitignore 已更新 |
| JWT_SECRET 硬编码默认值 | ✅ 已添加 `validate_security()` 启动校验 |
| 配置文件未被加载 | ✅ 已支持 `ENV_FILE` 环境变量 |
| Alembic 重复 FK 约束 | ✅ 第二个迁移已改为 no-op |
| init_db 使用 create_all | ✅ 已改为仅 DEBUG 模式 |
| 前端 SSE/上传/导出缺少 Authorization 头 | ✅ 已通过 `fetchWithAuth` 修复 |
| 前端 fetchJSON headers 可被覆盖 | ✅ 已修复展开顺序 |
| 前端 401 未更新 AuthContext | ✅ 已添加 `setOnUnauthorized` 全局回调 |
| Docker 后端容器以 root 运行 | ✅ 已添加 `appuser` (UID 1001) |

**第二轮 P1 修复验证（7/7 已确认修复）：**

| v2 P1 问题 | v3 验证结果 |
|-----------|-----------|
| 认证端点限流 | ✅ register 5/min, login 10/min |
| 登录时序攻击防护 | ✅ `_DUMMY_HASH` 恒定时间比较 |
| Sentry PII 可配置 | ✅ `send_default_pii=settings.SENTRY_SEND_PII` |
| memory_store 事务 | ✅ 改用 `flush()` |
| health check 信息泄露 | ✅ 仅返回 `{"status": "ok"}` |
| CSP 收紧 | ✅ 移除 `unsafe-inline`（后端中间件层） |
| rate_limiter Redis 支持 | ✅ `RATE_LIMIT_STORAGE_URI` |

---

## 第三轮新发现问题汇总

### 按严重程度分类

| 优先级 | 后端 | 前端 | DevOps | 合计（去重后） |
|--------|------|------|--------|--------------|
| P0（致命） | 3 | 0 | 5 | **8** |
| P1（重要） | 7 | 6 | 15 | **22**（去重后约 18） |
| P2（建议） | 12 | 25 | 15 | **40** |
| **合计** | **22** | **31** | **35** | **66** |

---

## P0 级问题详述（必须立即修复）

### P0-1: 初始 Alembic 迁移未创建任何表 — 生产部署必定失败

**文件**: `backend/alembic/versions/2026_07_13_0855-f65673b6a290_initial_schema.py`
**问题**: 迁移名为 "initial schema" 但仅执行 `op.add_column()` 添加 `owner_id`，无任何 `op.create_table()`。生产环境（DEBUG=False）跳过 `create_all`，运行 `alembic upgrade head` 时 `add_column` 会因目标表不存在而失败。
**影响**: 生产环境首次部署完全阻断
**修复**: 重写初始迁移，使用 `op.create_table()` 创建全部 9 张表

### P0-2: LLM 设置端点无管理员授权 — 可窃取 API Key

**文件**: `backend/app/api/settings.py:99-135`
**问题**: `update_llm_settings` 和 `test_llm_connection` 仅要求普通认证，任何注册用户可修改全局 LLM 配置。攻击者可将 `base_url` 改为自己控制的服务器，调用 `test_connection` 时 API Key 通过 `Authorization: Bearer` 头发送到攻击者服务器。
**影响**: API Key 泄露，产生费用
**修复**: 添加管理员角色检查，或改为环境变量配置

### P0-3: SlowAPIMiddleware 未添加 — default_limits 完全无效

**文件**: `backend/app/main.py:46-53`
**问题**: `rate_limiter.py` 配置了 `default_limits`，但 `main.py` 未添加 `SlowAPIMiddleware`。约 30 个端点（projects、worldview、outline、chapters 读取、export、settings 等）完全无限流保护。
**影响**: 接口可被滥用/DoS
**修复**: `app.add_middleware(SlowAPIMiddleware)`

### P0-4: 前端 Docker 容器以 root 运行

**文件**: `frontend/Dockerfile`
**问题**: 生产阶段使用 `nginx:1.27-alpine` 但未添加 `USER nginx`，nginx 主进程以 root 运行。v2 P0-10 标注"两个 Dockerfile"但前端未修复。
**修复**: 添加 `USER nginx` 或使用 `nginxinc/nginx-unprivileged` 镜像

### P0-5: CI 流水线缺少环境变量 — 测试必定失败

**文件**: `.github/workflows/ci.yml`
**问题**: CI 未设置 `ENV_FILE`、`DEBUG=true` 或 `JWT_SECRET`。`config.py` 的 `validate_security()` 在 DEBUG=False 且 JWT_SECRET 为空时抛出 RuntimeError，测试导入 `from app.main import app` 将直接崩溃。
**影响**: CI 流水线从第一次运行就失败
**修复**: CI 中添加 `env: ENV_FILE: .env.dev` 或 `DEBUG: "true"`

### P0-6: .github/ 目录未提交到 Git

**问题**: `git status` 显示 `.github/` 为 untracked。CI 配置存在于本地但从未 commit/push，推送到 GitHub 前完全不生效。
**修复**: `git add .github/ && git commit && git push`（需 GitHub Token 有 workflow scope）

### P0-7: docker-compose env_file 引用 gitignored 文件

**文件**: `docker-compose.yml:9-10`
**问题**: `env_file: backend/.env.prod` 引用的文件在 `.gitignore` 中被排除。全新 `git clone` 后此文件不存在，`docker compose up` 将报错。
**修复**: README 中明确说明需先 `cp backend/.env.example backend/.env.prod`，或改用 `.env` 文件 + 环境变量注入

### P0-8: 无 HTTPS/TLS 配置

**文件**: `frontend/nginx.conf`
**问题**: 仅监听 80 端口（HTTP），所有流量明文传输，包括 JWT 令牌和用户密码。无 443 端口 TLS 配置、无证书挂载、无 HSTS 头。v2 P0-14 未修复。
**影响**: 中间人攻击可截获认证凭据
**修复**: 添加 443 端口 TLS 配置模板（或至少提供配置文档和证书挂载说明）

---

## P1 级问题汇总（上线前应修复）

### 后端（7 项）

| # | 文件 | 问题 |
|---|------|------|
| 1 | chapters.py:366-370 | SSE 流式生成新 session 不重新验证项目所有权（TOCTOU） |
| 2 | community.py:341-356 | 点赞 API 无认证 + 无去重，任何人可无限刷赞 |
| 3 | community.py:249 | 浏览量每次请求 +1，无 IP/时间窗口去重 |
| 4 | alembic/env.py:41 | psycopg2 未在 requirements.txt，PostgreSQL 环境 Alembic 会失败 |
| 5 | config.py | LOG_LEVEL 未在 Settings 中声明，.env 中的 LOG_LEVEL 被忽略 |
| 6 | pytest.ini / conftest.py | 干净环境（CI）运行 pytest 因 JWT_SECRET 校验崩溃 |
| 7 | worldview.py:116-118 | 文件上传先全读再检查大小，可被 OOM 攻击 |

### 前端（6 项）

| # | 文件 | 问题 |
|---|------|------|
| 1 | api.ts:67-74 | 401 双重处理 — clearAuthToken + onUnauthorized 后未 return，错误继续传播 |
| 2 | ChapterWriter.tsx | 缺失 AbortController — SSE 流无法取消，组件卸载时内存泄漏 |
| 3 | Community.tsx:98 | 分页 offset 使用 data.length 而非 newOnes.length（v2 遗留未修复） |
| 4 | frontend/Dockerfile:26 | `wget --spider` 在 BusyBox/Alpine 下不兼容，HEALTHCHECK 持续失败 |
| 5 | nginx.conf:20 | CSP `connect-src 'self'` 阻断 Sentry 事件上报 |
| 6 | nginx.conf:20 | CSP `script-src 'unsafe-inline'` 降低 XSS 防护 |

### DevOps（15 项）

| # | 文件 | 问题 |
|---|------|------|
| 1 | backend/Dockerfile | 非多阶段构建，build-essential 残留（约 200MB+） |
| 2 | 两个 Dockerfile | 基础镜像未锁定 patch 版本 |
| 3 | requirements.txt | 8 个依赖使用 `>=` 未锁定 |
| 4 | docker-compose.yml | 前端容器无资源限制 |
| 5 | docker-compose.yml | 无 PostgreSQL 服务定义 |
| 6 | .env.prod | DATABASE_URL 为占位符，无 fail-fast 校验 |
| 7 | README.md | 无 HTTPS/TLS 部署说明 |
| 8 | README.md | 无数据库备份策略说明 |
| 9 | README.md | 无回滚方案 |
| 10 | README.md | 无生产部署 checklist |
| 11 | 全局 | 无监控告警配置文档 |
| 12 | 全局 | 无环境变量检查清单 |
| 13 | 全局 | 无数据库备份策略 |
| 14 | nginx.conf | 缺少 HSTS 头 + server_tokens off |
| 15 | docker-compose.yml | 缺少 init:true + security_opt |

---

## 五维度详细评估

### 1. 功能完整性（75/100）

**已实现的核心功能：**
- ✅ 用户注册/登录/JWT 认证
- ✅ 项目 CRUD + 所有权隔离
- ✅ 世界观创建/导入（手动/文档/混合）
- ✅ 大纲生成 + 确认流程
- ✅ 章节生成（单章 SSE + 批量生成）
- ✅ 字数配置 + 一键生成
- ✅ 社区浏览/上传/详情/点赞
- ✅ 小说导出（txt/markdown）
- ✅ LLM 设置管理 + 测试连接
- ✅ 进度监控面板

**缺失/不完整：**
- ⚠️ 点赞无去重（可刷赞）
- ⚠️ 浏览量无去重（刷新即 +1）
- ⚠️ 共创世界观功能未实现
- ⚠️ 内容审核功能未实现
- ⚠️ temperature=0.85 硬编码（忽略用户配置）
- ⚠️ batch 生成 total_words 始终为 0

**结论**: 核心功能链路完整可运行，但社区互动和批量生成统计有缺陷。

### 2. 代码质量（72/100）

**优点：**
- 模块化清晰：8 个 API 模块 + 6 个核心引擎模块
- 异步架构正确（async/await 一致使用）
- 日志框架完整（彩色控制台 + request_id）
- 全局异常处理（HTTPException/ValueError handler）
- 类型注解基本覆盖
- 前端 TypeScript 零错误

**问题：**
- ⚠️ SSE 流式生成 TOCTOU（所有权检查在新 session 不重新验证）
- ⚠️ 缺少 (project_id, chapter_num) 唯一约束（并发创建重复章节风险）
- ⚠️ datetime.utcnow 已弃用（Python 3.12+，Docker 使用 3.13）
- ⚠️ httpx 每次创建新 AsyncClient（无连接池复用）
- ⚠️ LLM 客户端无重试逻辑
- ⚠️ 前端缺少 AbortController（SSE 流无法取消）
- ⚠️ 前端社区分页 Bug 仍未修复

**结论**: 代码可读性和结构良好，但存在并发安全和资源管理隐患。

### 3. 测试覆盖（73/100）

**当前状态：**
- ✅ 127 项测试全部通过（85 unit + 42 integration）
- ✅ 覆盖核心引擎逻辑（worldview_parser、pacing_planner、style_engine、consistency_checker）
- ✅ API 集成测试覆盖 23 个端到端场景
- ✅ 认证和访问控制测试（401/403 隔离）
- ✅ TypeScript 类型检查零错误

**缺失：**
- ❌ CI 流水线未激活（.github 未提交 + 缺少环境变量）
- ❌ 前端零自动化测试
- ❌ 无测试覆盖率报告（未使用 pytest-cov）
- ❌ 无性能测试/压力测试
- ❌ 无安全扫描（safety/pip-audit/bandit）
- ❌ 干净环境 pytest 会崩溃（缺少 DEBUG=true）
- ❌ 初始迁移不建表 → 集成测试未覆盖生产部署路径

**结论**: 后端测试质量良好但 CI 未激活，前端测试完全缺失，无法保证回归安全。

### 4. 性能与安全（48 + 58 = 综合评估）

**性能（48/100）：**
- ⚠️ SlowAPIMiddleware 未注册 → default_limits 无效，约 30 个端点无限流
- ⚠️ SQLite 单文件数据库，不支持并发写入（生产级需 PostgreSQL）
- ⚠️ httpx 每次请求创建新客户端（无连接复用）
- ⚠️ LLM 调用无重试/无超时控制策略
- ⚠️ 社区 random 查询使用全表 ORDER BY RANDOM()
- ⚠️ settings_store 每次读取整个文件
- ✅ Redis 限流支持已添加（但未配置使用）
- ✅ SSE 流式传输正确（proxy_buffering off）

**安全（58/100）：**
- ✅ JWT_SECRET 启动校验（生产未设则拒绝启动）
- ✅ 密码 bcrypt 哈希 + 时序攻击防护
- ✅ 所有写操作需认证 + 所有权检查
- ✅ 前端所有请求携带 Authorization 头
- ✅ 401 全局回调自动登出
- ✅ CSP 收紧（移除 unsafe-inline，后端中间件层）
- ✅ 安全 Headers 完整（X-Frame/X-Content-Type/XSS/Referrer）
- ⚠️ LLM 设置端点无管理员授权（可窃取 API Key）— **严重**
- ⚠️ 无 HTTPS/TLS（凭据明文传输）— **严重**
- ⚠️ 前端 Docker 以 root 运行
- ⚠️ 文件上传先全读再检查大小（OOM）
- ⚠️ 注册端点允许用户枚举
- ⚠️ JWT 缺少 iat/jti 声明

**结论**: 认证和访问控制已基本完善，但 LLM 设置暴露和 HTTPS 缺失是两个致命安全漏洞。

### 5. 部署准备（58/100）

**已就绪：**
- ✅ Docker 容器化（后端非 root + HEALTHCHECK）
- ✅ docker-compose 编排（depends_on condition + 资源限制 + 日志轮转）
- ✅ Alembic 迁移框架（但初始迁移不建表）
- ✅ Sentry 错误监控（DSN 为空时自动禁用）
- ✅ 环境变量分离（.env.dev/.env.prod/.env.example）
- ✅ GitHub Actions CI 配置（但未提交 + 缺少环境变量）
- ✅ README.md 项目文档
- ✅ .gitignore 完整

**缺失（P0 阻塞）：**
- ❌ 初始迁移不创建表 → 生产部署 `alembic upgrade head` 失败
- ❌ CI 未提交到 Git + 缺少环境变量 → 自动化测试不可用
- ❌ env_file 引用 gitignored 文件 → 全新克隆部署失败
- ❌ 无 HTTPS/TLS 配置 → 凭据明文传输

**缺失（P1 重要）：**
- ❌ 无回滚方案文档
- ❌ 无数据库备份策略
- ❌ 无监控告警配置
- ❌ 无生产部署 checklist
- ❌ 无环境变量检查清单
- ❌ 后端 Dockerfile 非多阶段构建（镜像过大）
- ❌ 依赖版本未完全锁定
- ❌ 无 PostgreSQL 服务定义
- ❌ 前端 Docker HEALTHCHECK 不兼容 Alpine

**结论**: 部署基础设施框架已搭建，但存在 4 个阻断性问题和多个重要缺失项，无法直接部署到生产环境。

---

## 上线阻塞项清单

### 必须修复后方可上线（P0，共 8 项）

| # | 问题 | 阻断原因 | 修复复杂度 |
|---|------|---------|-----------|
| 1 | 初始迁移不创建表 | 生产部署必定失败 | 中（需重写迁移） |
| 2 | LLM 设置无管理员授权 | API Key 可被窃取 | 中（需添加 admin 角色） |
| 3 | SlowAPIMiddleware 未添加 | 大部分端点无限流 | 低（一行代码） |
| 4 | 前端 Docker 以 root 运行 | 容器提权风险 | 低（添加 USER） |
| 5 | CI 缺少环境变量 | CI 测试必定失败 | 低（添加 env） |
| 6 | .github 未提交 | CI 不生效 | 低（git add + push） |
| 7 | env_file 引用 gitignored 文件 | 全新克隆部署失败 | 低（文档说明） |
| 8 | 无 HTTPS/TLS 配置 | 凭据明文传输 | 高（需证书+配置） |

### 上线前应修复（P1，共 18 项，详见上文）

---

## 修复路线图

| 阶段 | 内容 | 预计问题数 | 目标 |
|------|------|-----------|------|
| 立即修复 | P0 全部 8 项 | 8 | 生产部署不阻断 |
| 上线前 | P1 安全 + 前端 + DevOps | 18 | 安全合规 + 可运维 |
| 灰度阶段 | P2 代码质量 + UX + 构建 | 40 | 体验优化 |

### 修复 P0 后预计可达

- ✅ Staging 环境部署
- ✅ 小范围灰度（可信用户）
- ❌ 公开上线（仍需 HTTPS + 备份 + 监控）

---

## 风险评估

| 风险 | 影响 | 概率 | 说明 |
|------|------|------|------|
| 生产部署失败 | 阻断 | 必然 | 初始迁移不建表 |
| API Key 窃取 | 费用/数据泄露 | 高 | LLM 设置无管理员授权 |
| 接口滥用/DoS | 服务不可用 | 高 | 限流中间件未注册 |
| 中间人攻击 | 凭据泄露 | 高 | 无 HTTPS |
| CI 回归风险 | 质量无保障 | 必然 | CI 未激活 |
| 容器提权 | 主机被攻破 | 中 | 前端 root 运行 |

---

*本报告基于 2026-07-14 commit 45f3b8d 的代码状态生成。建议修复全部 P0 后进行第四轮评估。*
