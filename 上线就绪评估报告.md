# 满分小说 — 上线就绪评估报告

> 评估日期：2026-07-13  
> 评估版本：当前开发分支（无 Git 版本控制）  
> 评估方法：代码审查 + 配置分析 + 架构评审 + 安全扫描

---

## 总体评估结论

### 不建议直接上线

当前项目处于 **MVP 原型阶段**，核心功能链路已跑通，但在安全性、测试覆盖、部署基础设施三个维度存在系统性缺失。建议归类为 **内部演示/Beta 测试版本**，需完成下文列出的 P0 级修复后方可进入小范围灰度。

---

## 一、功能完整性评估

### 已实现的核心功能

| 模块 | 功能点 | 状态 | 备注 |
|------|--------|------|------|
| 世界观管理 | 手动创建（7 类要素模板） | ✅ 完成 | 角色行为/地理/势力/体系/历史/矛盾/特殊设定 |
| 世界观管理 | 文档导入（AI 提取） | ✅ 完成 | 支持 .txt/.md/.doc/.docx，多编码兼容 |
| 世界观管理 | 混合模式 | ✅ 完成 | 导入 + 手动追加 |
| AI 生成引擎 | 大纲生成 | ✅ 完成 | LLM 生成 story_arc + 章节大纲 |
| AI 生成引擎 | 渐进式揭示 | ✅ 完成 | 三阶段（引入/展开/深入） pacing planner |
| AI 生成引擎 | 7 种网文类型风格 | ✅ 完成 | 玄幻/都市/科幻/武侠/仙侠/悬疑/言情 |
| 章节写作 | SSE 流式生成 | ✅ 完成 | 单章逐字流式 + 批量生成 |
| 章节写作 | 字数配置 | ✅ 完成 | 总字数自动分配 + 单章 override |
| 章节写作 | 一键批量生成 | ✅ 完成 | 进度条 + 结果日志 |
| 章节写作 | 章节编辑 | ✅ 完成 | 生成后可编辑标题和正文 |
| 社区功能 | 小说上传/编辑/删除 | ✅ 完成 | 关联项目自动填充统计 |
| 社区功能 | 标签系统 | ✅ 完成 | 动态增删 + 按使用量排序 |
| 社区功能 | 无限滚动 + 随机加载 | ✅ 完成 | IntersectionObserver + exclude 去重 |
| 社区功能 | 点赞 + 阅读计数 | ✅ 完成 | |
| 社区功能 | 共创开关 | ✅ 完成 | allow_cocreation 字段 |
| 设置管理 | LLM API 配置 | ✅ 完成 | 6 个预置服务商 + 自定义 |
| 导出 | TXT / Markdown | ✅ 完成 | |

### 遗漏的关键模块

| 缺失功能 | 严重度 | 说明 |
|----------|--------|------|
| **用户认证与授权** | P0 | 无登录系统、无用户体系，任何人可访问/修改/删除任何项目和小说 |
| **共创世界观实际功能** | P1 | allow_cocreation 仅为开关字段，无实际"基于他人世界观创建项目"的流程 |
| **内容审核/举报** | P1 | 社区功能无内容审核机制，存在违规内容风险 |
| **搜索功能** | P2 | 社区仅支持标签筛选，无全文搜索 |
| **用户个人主页** | P2 | 无作者页面、作品集展示 |
| **通知系统** | P2 | 无点赞/评论通知 |
| **评论功能** | P2 | 社区小说无评论/讨论区 |
| **数据备份/恢复** | P2 | 无数据导出/导入机制 |

**功能完整性评分：70/100** — 核心创作链路完整，但用户体系和社区治理缺失。

---

## 二、代码质量评估

### 优势

- **三轮深度审查**：累计修复 27 个 bug（后端 21 + 前端 6），涵盖 P0/P1/P2 全级别
- **TypeScript 严格模式**：`tsc --noEmit` 零错误
- **Pydantic 校验**：所有 API 入参有类型约束和范围校验
- **异步架构**：全链路 async/await，SQLAlchemy 异步引擎
- **SSE 错误处理**：流式生成中断时保存部分内容 + 回滚事务
- **代码分层清晰**：API → Core → Models → Schemas 四层分离

### 技术债务

| 问题 | 严重度 | 说明 |
|------|--------|------|
| **无 Git 版本控制** | P0 | 项目未初始化 Git，无任何版本历史，无法追溯变更、无法回滚 |
| **无日志框架** | P1 | 全项目无 logging 配置，错误仅通过 HTTPException 返回，生产环境无法排查问题 |
| **Debug 模式硬编码为 True** | P1 | `config.py` 中 `DEBUG: bool = True`，SQLAlchemy echo 会打印所有 SQL |
| **无 React Error Boundary** | P1 | 前端无全局错误边界，组件异常会导致白屏 |
| **无统一异常处理中间件** | P1 | 后端无 FastAPI exception_handler，未捕获异常返回 500 裸错误 |
| **API Key 明文存储** | P1 | `llm_settings.json` 明文保存 API Key，无加密 |
| **LLMClient 每次请求 reload** | P2 | `chat()`/`chat_stream()` 每次都读文件加载配置，高频调用下有 I/O 开销 |
| **word_count 使用 len() 而非中文计数** | P2 | `len(full_content)` 对中文按字符数计算，与用户认知的"字数"一致但需明确 |
| **社区 random 端点全表扫描** | P2 | `get_random_novels` 先查所有 ID 再 random.sample，数据量大时有性能问题 |

**代码质量评分：55/100** — 业务逻辑质量尚可，但工程基础设施严重缺失。

---

## 三、测试覆盖情况评估

### 当前状态

| 测试类型 | 覆盖率 | 说明 |
|----------|--------|------|
| 单元测试 | **0%** | 无任何测试文件，未配置 pytest |
| 集成测试 | **0%** | 无 API 级别测试 |
| 前端组件测试 | **0%** | 未配置 Jest / Vitest / React Testing Library |
| 端到端测试 | **0%** | 无 Playwright / Cypress 自动化测试 |
| 手动测试 | 有 | 通过 curl 手动验证 API 端点，非自动化、不可回归 |

### 风险评估

- **回归风险极高**：每次修改无法验证是否引入新 bug
- **核心引擎无保护**：pacing_planner、worldview_parser、consistency_checker 等核心算法无测试覆盖
- **SSE 流式逻辑无测试**：批量生成、断线重连等场景完全未测试
- **前端交互无测试**：无限滚动、表单校验、SSE 消费逻辑无测试

**测试覆盖评分：5/100** — 仅有手动验证，几乎无自动化测试。

---

## 四、性能与稳定性评估

### 当前状态

| 维度 | 状态 | 说明 |
|------|------|------|
| 数据库 | ⚠️ SQLite | 单文件数据库，不支持并发写入，生产环境会锁库 |
| 连接池 | ❌ 未配置 | SQLAlchemy async engine 使用默认参数，未调优 |
| 速率限制 | ❌ 无 | 无 slowapi / 限流中间件，LLM 调用无频率控制 |
| 缓存层 | ❌ 无 | 标签列表、小说列表等热数据无缓存 |
| 性能测试 | ❌ 未执行 | 无压测报告，不知道系统承载能力 |
| LLM 调用超时 | ⚠️ 120s | httpx 超时 120s，长文本生成可能不够 |
| 内存管理 | ⚠️ 未验证 | SSE 长连接 + 大文本拼接，未验证内存峰值 |
| 负载测试 | ❌ 未执行 | 不知道并发上限 |

### 已知风险

1. **SQLite 并发瓶颈**：多个用户同时生成章节时，写锁会导致 `database is locked` 错误
2. **LLM API 调用阻塞**：批量生成 30 章时，每章 120s 超时，最差情况 60 分钟占用连接
3. **SSE 连接堆积**：无连接数限制，高并发下可能耗尽文件描述符
4. **全量加载随机**：`get_random_novels` 先取全表 ID 再采样，10 万条数据时内存和查询都会成问题

**性能与稳定性评分：30/100** — 未进行任何性能测试，SQLite 不适合生产并发。

---

## 五、安全性评估

### 安全检查清单

| 检查项 | 状态 | 风险说明 |
|--------|------|----------|
| 用户认证 | ❌ 无 | 无 JWT/OAuth/Session，所有 API 完全开放 |
| 用户授权 | ❌ 无 | 无角色/权限控制，任何人可删除他人数据 |
| API Key 存储 | ❌ 明文 | `data/llm_settings.json` 明文存储，文件可读 |
| CORS 配置 | ⚠️ 仅 localhost | 生产环境需配置实际域名 |
| Debug 模式 | ❌ 开启 | `DEBUG=True` 暴露 SQL 日志和堆栈信息 |
| HTTPS | ❌ 无 | 无 TLS 配置，数据传输明文 |
| 速率限制 | ❌ 无 | 无防暴力调用机制，LLM API 可被滥用 |
| 输入消毒 | ⚠️ 仅 Pydantic | 无 XSS 过滤、无 HTML 清理 |
| SQL 注入 | ✅ 安全 | SQLAlchemy ORM 参数化查询 |
| 文件上传 | ⚠️ 无大小限制 | `upload_worldview_file` 未限制文件大小 |
| 安全 Headers | ❌ 无 | 无 CSP / X-Frame-Options / X-Content-Type-Options |
| CSRF 防护 | ❌ 无 | 无 CSRF Token |
| 敏感数据泄露 | ❌ 有风险 | health check 返回 `llm_configured` 布尔值（低危） |

### 高危漏洞

1. **P0 — 任意删除漏洞**：`DELETE /api/projects/{id}` 和 `DELETE /api/community/novels/{id}` 无身份验证，任何人可删除任何数据
2. **P0 — API Key 泄露**：`GET /api/settings/llm` 端点返回 API Key 明文（如果前端需要回显），且存储文件无加密
3. **P0 — 无访问控制**：所有 CRUD API 无身份验证，等于完全公开的数据操作

**安全性评分：15/100** — 存在多个 P0 级安全漏洞，不具备上线条件。

---

## 六、部署准备状态评估

### 部署检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 版本控制 | ❌ 无 | 项目未初始化 Git，无 commit 历史 |
| Docker 容器化 | ❌ 无 | 无 Dockerfile / docker-compose.yml |
| CI/CD 流水线 | ❌ 无 | 无 GitHub Actions / GitLab CI 配置 |
| 环境分离 | ❌ 无 | 无 dev/staging/prod 配置分离 |
| 反向代理 | ❌ 无 | 无 nginx / Caddy 配置 |
| HTTPS 证书 | ❌ 无 | 无 TLS 配置 |
| 数据库迁移 | ❌ 无 | 使用 `create_all()` 自动建表，无 Alembic 迁移 |
| 日志收集 | ❌ 无 | 无 ELK / Grafana Loki 等日志系统 |
| 监控告警 | ❌ 无 | 无 Prometheus / Sentry / Uptime 监控 |
| 回滚方案 | ❌ 无 | 无版本控制、无蓝绿部署、无回滚流程 |
| 部署文档 | ❌ 无 | 仅有 .env.example，无部署手册 |
| 数据备份 | ❌ 无 | SQLite 文件无备份策略 |

**部署准备评分：10/100** — 几乎不具备任何生产部署基础设施。

---

## 七、综合评分与上线建议

### 评分汇总

| 维度 | 评分 | 状态 |
|------|------|------|
| 功能完整性 | 70/100 | 🟡 核心功能完成，用户体系缺失 |
| 代码质量 | 55/100 | 🔴 工程基础设施缺失 |
| 测试覆盖 | 5/100 | 🔴 几乎无自动化测试 |
| 性能与稳定性 | 30/100 | 🔴 未测试 + SQLite 瓶颈 |
| 安全性 | 15/100 | 🔴 多个 P0 漏洞 |
| 部署准备 | 10/100 | 🔴 无部署基础设施 |
| **综合** | **31/100** | **🔴 不具备上线条件** |

### 上线建议

#### 当前版本适用场景
- ✅ 内部演示/Demo 展示
- ✅ 小范围 Alpha 测试（可信用户）
- ❌ 公开上线
- ❌ 生产环境部署

#### 上线前必须完成（P0 阻塞项）

1. **初始化 Git 版本控制** — 所有后续工作前提
2. **实现用户认证系统** — JWT + 用户注册/登录
3. **实现 API 访问控制** — 所有写操作需验证身份和所有权
4. **API Key 加密存储** — 使用环境变量或加密存储，不落盘明文
5. **关闭 Debug 模式** — 生产环境 `DEBUG=False`
6. **切换到生产级数据库** — PostgreSQL / MySQL
7. **配置 HTTPS** — TLS 证书 + 反向代理
8. **添加速率限制** — 防止 LLM API 滥用
9. **添加文件上传大小限制** — 防止大文件攻击
10. **Docker 容器化** — 可重复部署

#### 上线前建议完成（P1 重要项）

11. **核心模块单元测试** — 覆盖 pacing_planner / worldview_parser / consistency_checker
12. **API 集成测试** — 覆盖所有端点的正常 + 异常路径
13. **配置日志框架** — Python logging + 结构化日志
14. **React Error Boundary** — 全局错误边界
15. **统一异常处理中间件** — FastAPI exception_handler
16. **数据库迁移工具** — 引入 Alembic
17. **环境配置分离** — dev / staging / prod 配置
18. **基础监控** — 至少接入 Sentry 错误监控

#### 上线后持续改进（P2 优化项）

19. **CI/CD 流水线** — 自动化构建 + 测试 + 部署
20. **性能压测** — 确定系统承载能力
21. **共创世界观功能实现** — allow_cocreation 从开关变为实际流程
22. **内容审核机制** — 社区内容过滤
23. **搜索功能** — 全文搜索
24. **数据备份策略** — 定时备份 + 恢复演练

### 风险提示

| 风险 | 影响 | 概率 |
|------|------|------|
| 任意用户删除数据 | 数据丢失 | 极高（当前无防护） |
| LLM API Key 泄露 | 经济损失 | 高（明文存储） |
| SQLite 锁库 | 服务中断 | 高（并发写入时） |
| 无回滚能力 | 变更不可逆 | 高（无版本控制） |
| 无监控 | 故障不可发现 | 高 |
| 无测试 | 回归风险 | 高 |

---

*本报告基于 2026-07-13 的代码状态生成。建议完成上述 P0 项后重新评估。*
