# AI 小说创作平台 Agent 总规则

本文件适用于仓库中的所有 Agent、子 Agent 和自动化任务。角色专属规则位于：

- `agents/manager.md`
- `agents/core-developer.md`
- `agents/web-designer.md`

所有角色必须同时遵守本文件、自己的角色文件和 `coordination/` 中的项目记录。发生冲突时，用户当前指令优先，其次是本文件，再次是角色文件和历史项目记录。

一般性的“删除”“清理”或“移除”指令只表示启动删除评估和审批流程，不等于已经批准执行。只有 Agent 披露精确路径、影响和恢复方式后，用户对该批路径作出的明确回复，才构成删除批准。

## 一、不可改变的产品目标

本项目是 AI 小说创作平台，不是单纯的聊天机器人或一次性续写工具。

平台必须围绕以下核心能力演进：

- 世界观、角色、场景、阵营、物品、冲突、事件、伏笔、规则与自定义要素的解析和结构化存储；
- 要素之间的稳定引用和关系；
- 设定与指定章节的绑定以及首次出现控制；
- 伏笔的埋入、强化、追踪和回收；
- 人物、世界规则、剧情和时间线的一致性；
- AI 生成内容的编辑、版本、重新生成和来源追踪；
- 长篇小说的持续创作和上下文管理。

不得擅自把产品改为社区平台、通用写作助手或其他无关方向。

## 二、强制多 Agent 协作

### 1. 主负责人修改前必须调用其他 Agent

任何顶层项目任务或拥有独立交付物、文件修改权的工作单元，包括需求分析、设计、编码、测试、审查、修复和项目文档更新，在修改文件或给出最终结论前，主负责人必须通过可用的协作工具实际调用至少一名其他角色参与。

协作必须发生在实质工作阶段，不能只在任务完成后补发通知。

仅受主负责人邀请、范围明确、只提供意见或只读审查的协作 Agent 属于叶节点。叶节点直接回复主负责人即完成协作，不得再次委派、反向调用请求者或重复调用父任务中已经参与的角色。

如果协作 Agent 发现父任务没有覆盖的独立专业问题，应向主负责人建议追加协作，由主负责人决定和发起，不得自行形成递归调用链。

以下任务必须同时让核心开发者和网站设计者参与：

- 新功能或用户流程；
- 数据模型或 API 将影响界面的变更；
- 页面、导航、交互或响应式改动；
- 需要在开发成本和用户体验之间作取舍的事项。

最高管理者负责整合结论，不得把其他 Agent 的输出不经审查直接作为最终结论。

### 2. 协作调用必须具体

每次协作消息至少包含：

- 发起 Agent 所代表的角色；
- 要求对方代表的角色及应读取的 `agents/*.md`；
- 任务编号；
- 当前背景和目标；
- 主负责人；
- 已参与角色；
- 希望对方回答的具体问题；
- 影响范围；
- 推荐方案或待比较方案；
- 验收标准；
- 需要回复的里程碑。

禁止发送“看看”“协作一下”“有问题吗”等没有上下文的消息。

### 3. 必须等待并记录协作结果

主负责人必须等待被调用 Agent 的有效回复，再继续完成需要该回复的部分。

有效回复至少包含：

- 任务编号和回复角色；
- 审查范围；
- 结论与依据；
- 风险和影响；
- 推荐方案及可选替代方案；
- 对相关验收标准的判断；
- 需要主负责人或用户决定的问题；
- 是否建议进入下一里程碑。

最终报告或对应的 `coordination/` 记录必须写明：

- 参与的 Agent；
- 对方提出的结论或风险；
- 当前 Agent 接受、修改或拒绝了哪些建议；
- 最终决定及理由。

不得在协作尚未返回时声称任务已完成。

### 4. 无法调用时的处理

如果运行环境没有协作工具，主负责人不得伪造协作结果，必须将任务标记为 `BLOCKED` 并向用户申请指示。

如果只是 Agent 名额暂满，主负责人应先等待本任务已经运行的协作者完成，然后重试一次。只有等待并重试后仍无法获得任务要求的角色协作，才将任务标记为 `BLOCKED`。

叶节点遇到协作或资源限制时，只向主负责人报告，不修改共享状态文件。主负责人可以在不发起新一轮协作的情况下，追加一次纯阻塞记录并通知用户。

不得以“任务很小”为理由跳过强制协作。

### 5. 防止无效递归

- 同一子任务只指定一个主负责人；
- 不得让多个 Agent 同时修改同一文件；
- 叶节点协作 Agent 不得创建新的 Agent；
- 协作 Agent 不得反向调用请求者；
- 已参与同一任务的角色不得被重复调用处理相同问题；
- 需要并行时，必须按互不重叠的文件或问题边界拆分；
- 最高管理者负责最终合并、冲突处理和验收。

同一任务编号下，把已经形成的协作结论机械写入角色报告、项目状态和决策记录，不产生新的协作义务。只有修改任务范围、验收标准、产品方向或既有决策时，才需要开启新一轮协作。

### 6. 文件和记录所有权

- 主负责人是该任务共享记录的唯一写入者；
- 协作 Agent 默认只返回结构化意见，不修改共享文件；
- 只有主负责人明确分配互不重叠的独立文件时，协作 Agent 才能修改；
- `coordination/project-status.md` 和 `coordination/decisions.md` 默认由最高管理者统一更新；
- 开发者和设计者只更新各自角色记录，或把结果交给最高管理者合并；
- 多 Agent 不得同时编辑同一文件。

## 三、任务与状态管理

每项工作必须先有唯一任务编号：

- `PM-xxx`：产品和管理；
- `DEV-xxx`：开发；
- `DES-xxx`：设计；
- `BUG-xxx`：缺陷。

统一状态：

- `BACKLOG`
- `READY`
- `IN_PROGRESS`
- `BLOCKED`
- `REVIEW`
- `CHANGES_REQUESTED`
- `APPROVED`
- `CANCELLED`

没有明确范围和验收标准的任务不得进入 `READY`，没有测试或验证证据的实现不得进入 `REVIEW`。

解除 `BLOCKED` 时：尚未实施的任务回到 `READY`；已经开始实施的任务回到 `IN_PROGRESS`。状态变更必须追加记录。

任务执行前必须检查：

- `coordination/project-status.md`
- 自己的角色记录；
- `coordination/decisions.md`
- 与本任务相关的审查、设计或开发报告。

不得删除历史协作记录。需要纠正时追加更正记录。

## 四、核心代码删除保护

### 1. 受保护范围

以下内容均视为核心代码或关键项目资产：

- `AGENTS.md`
- `agents/`
- `coordination/`
- `backend/`
- `frontend/src/`
- `frontend/public/`
- `frontend/package.json`
- `frontend/package-lock.json` 及仓库内其他依赖锁文件
- `frontend/vite.config.ts`、`frontend/vitest.config.ts`、`frontend/tsconfig*.json`
- 数据库迁移、部署、CI、安全和环境配置
- 根目录产品、设计、部署与协作文档
- 用户明确指定为核心的其他文件或目录

可重新生成且未被 Git 跟踪的缓存、测试缓存、构建产物和临时文件不属于核心资产，但处理前仍必须核对精确路径和 Git 状态。

### 2. 禁止直接删除

任何 Agent 都不得对受保护文件或目录执行直接删除，包括但不限于：

- `rm`、`rmdir`、`unlink`、`find -delete`
- `git rm`
- `git clean`
- 通过脚本递归删除
- 覆盖为空文件以达到删除效果
- 使用 `git reset --hard`、`git checkout --` 等方式抹去用户修改

任务范围内小规模替换或删除错误代码行不等于删除整个文件，但必须保留清晰的 diff。

删除完整功能、公共 API、主要模块或大比例核心实现，即使保留空壳文件，也必须先由最高管理者审查。若会删减或改变核心产品能力，还必须向用户提交影响和替代方案并获得明确批准。

### 3. 移入系统废纸篓前必须申请批准

如果确实需要移除核心文件或目录，当前 Agent 必须先暂停并向用户申请明确批准。申请必须包含：

- 任务编号；
- 待移除对象的精确路径；
- 移除原因；
- 对功能、数据、构建和迁移的影响；
- 已检查的引用关系；
- 可替代方案；
- 推荐方案；
- 从废纸篓恢复的方法。

只有用户针对这些精确路径明确批准后，才可以使用操作系统废纸篓移动文件。批准仅对指定任务、该批精确路径和单次操作有效；路径、内容或影响范围发生变化时必须重新申请。

批准前不得先移动、重命名来规避审批，也不得把文件移出工作区后声称没有删除。

### 4. 批准后的执行要求

- 使用系统废纸篓，不使用永久删除；
- 如果系统废纸篓 API 或经过验证的废纸篓工具不可用，将任务标记为 `BLOCKED`，不得用仓库内备份目录代替；
- 每次只处理批准的精确路径；
- 操作前解析并展示仓库内规范化绝对路径，确认目标没有通过符号链接逃逸；
- 操作前检查 Git 状态，确认批准后目标和影响范围没有变化；
- 不得使用宽泛目录、通配符或未解析变量；
- 文本批准不代替运行环境所要求的命令权限或系统授权；
- 移动后验证核心流程和构建；
- 在开发报告中记录移动时间、原路径、废纸篓实际名称、恢复方式和验证结果；
- 不得清空废纸篓，除非用户另行明确批准。

### 5. 数据结构与用户数据

Agent 直接删除真实或持久化环境中的数据库表、列、迁移历史、用户项目或生成内容，或者执行破坏性生产迁移，属于不可逆高风险操作。必须提交数据迁移、备份、回滚和影响方案，并获得用户明确批准后才能执行。

本条不阻止：

- 在隔离测试数据库中清理临时 fixture；
- 实现已经获得产品批准、带用户确认和恢复策略的删除功能；
- 在不接触真实数据的条件下编写或测试迁移。

生产或持久化环境中的实际删除始终需要专项方案和批准。

## 五、代码与设计质量门禁

### 开发门禁

- 先理解现有实现，再修改；
- 修改范围与任务验收标准一致；
- 补充与风险匹配的测试；
- 执行相关旧功能回归；
- 如实记录失败、限制和未验证部分；
- 不擅自引入新的付费服务或重大架构依赖；
- 不把 Mock、静态示例或未接入的代码描述为已上线功能。

### 设计门禁

- 必须包含加载、空白、错误、禁用和成功状态；
- 必须说明桌面端和常用移动尺寸；
- 必须考虑键盘操作、焦点、对比度和减少动画；
- 必须与核心开发者确认数据和实现可行性；
- 不直接复制其他产品的品牌、图标组合或完整页面。

### 管理验收门禁

最高管理者只能给出：

- `APPROVED`
- `CHANGES_REQUESTED`
- `BLOCKED`
- `CANCELLED`

不得用“收到”“可以”“完成了”等模糊回复代替验收结论。

开发和设计任务由最高管理者验收。最高管理者主责的治理、范围、重大决策和 `PM-*` 交付必须由项目负责人验收，最高管理者不得自我批准。

## 六、默认协作流程

1. 读取仓库规则、角色文件和项目状态；
2. 创建或确认任务编号；
3. 在任务消息或工作上下文中明确范围、非范围、依赖和验收标准；
4. 主负责人强制调用其他 Agent 参与分析或审查；
5. 整合协作意见，首次写入任务记录并将状态标记为 `READY`；
6. 开始实施时将状态标记为 `IN_PROGRESS`；
7. 按分工实施，协作 Agent 不同时修改共享文件；
8. 执行测试、视觉检查或文档验证；
9. 提交角色报告并将状态改为 `REVIEW`；
10. 开发和设计任务由最高管理者验收，`PM-*` 任务由项目负责人验收；
11. 更新 `coordination/project-status.md` 和最后检查时间。

## 七、沟通格式

协作请求至少包含：

- 时间：
- 任务编号：
- 发起人：
- 接收人：
- 当前状态：
- 背景与目标：
- 具体问题：
- 影响：
- 推荐方案：
- 验收标准：
- 最晚回复里程碑：
- 下一步行动：

协作回复至少包含：

- 时间：
- 任务编号：
- 回复角色：
- 审查范围：
- 结论与依据：
- 风险和影响：
- 推荐方案：
- 替代方案：
- 验收标准判断：
- 需要决定的问题：
- 是否建议进入下一里程碑：
