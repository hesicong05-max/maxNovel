# 项目状态

## 项目概况

- 最后检查时间：2026-07-30 16:19 CST
- 当前阶段：M1 统一设定模型与设定库
- 总体状态：有风险
- 本周期完成情况：`DEV-003A` 已推送并创建 Draft PR；两轮 PostgreSQL 差异已定位并本地修复，等待第三次 CI
- 当前完成度：现有续写原型可运行；相对新的核心 Beta 目标估算约 35%

## 已批准

- 任务编号：`PM-001`
- 完成内容：项目现状审查、差距矩阵、风险排序、里程碑和初始任务分配
- 验收结果：`APPROVED`，项目负责人于 2026-07-30 指示开始研发

- 任务编号：`PM-005`
- 完成内容：建立仓库级 Agent 规则和三个角色文件；根据核心开发者和网站设计者审查，补充叶节点终止规则、单向协作、记录所有权、系统废纸篓机械安全和管理任务禁止自批
- 验收结果：`APPROVED`，项目负责人于 2026-07-30 指示按该规则开始研发

- 任务编号：`DEV-001`
- 完成内容：CI 全量门禁、本地一键验证、项目删除文件一致性、环境文件说明与回归测试
- 验收结果：`APPROVED`；后端 477 项、前端 37 项、类型检查、生产构建与 Bandit 高危扫描通过

- 任务编号：`DES-001`
- 完成内容：桌面与 390px 移动端创作工作台信息架构、完整状态、响应式和可访问性基线
- 验收结果：`APPROVED`；开发可行性评审已完成

- 任务编号：`PM-002`
- 完成内容：MVP 能力范围、统一术语、核心流程、用户故事和 Beta 非目标
- 验收结果：`APPROVED`，项目负责人于 2026-07-30 指示“同意，继续吧”

- 任务编号：`DES-002`
- 完成内容：统一设定库、关系、来源、历史版本、合并、桌面与 390px 交互和完整状态
- 验收结果：`APPROVED`；技术草案已处理全部设计阻断项

- 任务编号：`DEV-002`
- 完成内容：统一领域模型、兼容迁移、版本、来源、关系、章节绑定和分阶段实施方案
- 验收结果：`APPROVED`，项目负责人于 2026-07-30 回复“批准”

## 发布进行中

- `DEV-003A-CI`：分支与 Draft PR #1 已创建；后端、前端及 PostgreSQL 迁移往返持续通过。连接池错误修复后，第二次 CI 暴露旧迁移把 JSON 列建成 `Text` 的历史兼容差异
- 影响：PostgreSQL 会将旧数组读成 JSON 字符串，原只读投影无法识别；不影响 Alembic 往返，但阻塞 Lore API 验收
- 推荐方案：兼容层受控解析历史 JSON 字符串并重跑 CI；物理列转换另列 `BUG-002`，未经批准不迁移真实数据
- 最晚交付点：本轮发布流程结束前

## 风险与阻塞

### DEV-003A PostgreSQL 门禁未执行

- 影响：SQLite 和纯逻辑测试无法完全证明 PostgreSQL 约束与 Alembic 往返迁移可靠，任务不能进入 `REVIEW`。
- 已采取措施：新增 PostgreSQL 16.4 CI 服务，覆盖升级至 head、降级到上一版本、再次升级和领域模型/API 测试；发布复核提出的无损兼容与 cursor 安全问题已修复并获复核 `APPROVED`；迁移往返通过；测试连接池和历史 Text JSON 兼容修复后本地 505 项后端测试通过。
- 推荐方案：提交并推送当前分支，运行真实 PostgreSQL CI。
- 是否需要负责人决策：否，提交与推送及 GitHub 登录均已完成授权。

### 本地环境密钥疑似暴露

- 影响：`backend/.env.dev` 与 `backend/.env.prod` 疑似含真实 LLM 密钥；文件虽被忽略且未被 Git 跟踪，但密钥本身可能仍有效。
- 已采取措施：未读取输出、复制或修改密钥；已核实两个文件均被 `.gitignore` 排除。
- 推荐方案：项目负责人立即在对应服务商撤销并轮换密钥，再只通过本地 ignored 文件或部署密钥管理配置。
- 是否需要负责人决策：需要负责人执行密钥轮换。

### 历史 JSON 列物理类型不一致

- 任务编号：`BUG-002`
- 影响：初始 Alembic 在 PostgreSQL 将多组 ORM `JSON` 字段建为 `Text`；SQLite 不暴露该差异，旧读取接口可能收到字符串而不是列表或对象。
- 已采取措施：`DEV-003A` 只读兼容层无损解析 worldview 历史字符串；未修改旧迁移或真实数据。
- 推荐方案：单独设计带数据预检、备份、可回滚的新增迁移，统一 worldview、outline、chapter 和 story memory JSON 列。
- 是否需要负责人决策：需要，涉及真实数据类型转换。

### 删除事务的极端失败补偿

- 影响：数据库提交成功后若运行时暂存目录清理发生系统级异常，项目记录已删除但暂存副本可能残留。
- 已采取措施：正常、归档失败、数据库失败恢复和重复删除路径均有测试；异常会返回明确错误并记录 critical 日志。
- 推荐方案：M5 `DEV-012` 增加暂存残留清扫、指标和告警。
- 是否需要负责人决策：否，作为发布门禁风险跟踪。

### 移动端核心导航缺失

- 影响：小屏用户登录后无法通过侧栏进入主要功能。
- 已采取措施：列入 `DES-001` 和 `BUG-001`。
- 推荐方案：先完成移动导航方案，再实现响应式工作区。
- 是否需要负责人决策：否。

## 下一步计划

- 下一项任务：运行 `DEV-003A` 真实 PostgreSQL CI 并完成验收
- 负责人：最高管理者、核心开发者；项目负责人批准提交与推送
- 验收目标：PostgreSQL 迁移往返和领域模型/API 测试通过，旧 JSON 仍是事实源且无写入、切换或删除
- 下一步行动：提交和推送当前分支，跟踪 CI；通过后启动 `DEV-003B`。
