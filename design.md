# 小说世界观续写 Agent — 设计文档

## 1. 产品定位

一款 AI 驱动的网文续写工具，用户上传世界观架构 + 选择网文类型 + 配置输出参数，
Agent 自动生成符合网文写作范式的章节内容，并按照渐进式策略展开世界观要素。

### 核心差异化
- **不是**简单地把世界观喂给 LLM 让它写作
- **而是**像真正的网文作者一样：规划节奏 → 分配要素 → 控制信息密度 → 保证一致性

---

## 2. 系统架构（三层）

### 2.1 用户输入层
- **世界观架构上传**：支持结构化/半结构化世界观文档
- **网文类型选择**：玄幻、都市、科幻、武侠、仙侠、悬疑、言情等
- **输出参数配置**：总长度、单章字数、节奏偏好、风格强度

### 2.2 核心 Agent 引擎层
- **世界观解析引擎**：将上传的世界观解析为结构化要素（角色、地理、势力、体系、事件等），
  并为每个要素分配优先级（核心/重要/次要/背景）
- **节奏规划器**：根据总长度和要素数量，计算每个阶段/章节应揭示哪些要素，
  生成「世界观揭示时间线」
- **风格学习模块**：内置各类型网文的写作范式（对话方式、剧情节奏、爽点设计、
  金手指布局等），可参考用户提供的热门小说样本进行风格校准
- **一致性检查器**：跨章节检查角色行为一致性、时间线无矛盾、设定不冲突、
  伏笔追踪与回收
- **故事记忆库**：持久化已揭示要素、角色状态、剧情进度、伏笔队列、章节摘要

### 2.3 输出与交互层
- **章节生成**：按段落/章节流式输出
- **实时预览/编辑**：用户可修改生成内容，修改后 Agent 重新规划后续节奏
- **进度面板**：可视化世界观揭示进度、剩余要素、当前故事阶段
- **导出**：txt / docx / epub

---

## 3. 世界观渐进式揭示策略（核心模块）

### 3.1 三阶段揭示模型

| 阶段 | 章节范围 | 揭示比例 | 揭示内容 |
|------|---------|---------|---------|
| 引入期 | Ch.1 ~ Ch.3 | 10-15% | 主角处境、核心矛盾雏形、环境氛围 |
| 展开期 | Ch.4 ~ Ch.10 | 30-40% | 核心体系、势力关系、关键配角、金手指 |
| 深入期 | Ch.11+ | 60-70% | 深层设定、伏笔回收、世界观冲突 |

### 3.2 节奏引擎算法

```
输入：世界观要素总数 N + 用户选择输出总长度 L
计算：每章分配要素数 = f(N, L, 当前阶段权重)
约束：
  - 单章揭示要素 ≤ 阈值（防止信息倾倒）
  - 核心要素需跨章铺垫（至少3章前有暗示）
  - 伏笔间距 ≥ 3章
  - 设定描述 ≤ 单章总字数 20%
```

### 3.3 防信息倾倒规则
1. 每章设定描述不超过总字数 20%
2. 设定通过剧情/对话自然带出，不使用说明文段落
3. 新设定出现必须绑定具体情境（角色互动/事件触发）
4. 已揭示要素不重复解释

### 3.4 一致性保障规则
1. 角色行为符合既有性格设定
2. 时间线无矛盾
3. 地理/势力关系前后一致
4. 伏笔追踪：铺设 → 强化 → 回收

---

## 4. 写作风格参考系统

### 4.1 内置写作范式库
按网文类型预置：
- 对话模式（角色对话风格、对话与叙述比例）
- 剧情节奏（爽点密度、低谷-高潮曲线）
- 叙事视角（第一人称/第三人称限制/全知视角）
- 爽点设计（打脸节奏、升级节奏、反转频率）

### 4.2 用户可自定义
- 上传参考小说片段（几章节），Agent 提取风格特征
- 调整对话/叙述比例
- 选择爽点强度（温和/标准/密集）

---

## 5. 技术架构（待确认）

### 5.1 前端
- React + TypeScript
- 富文本编辑器（支持实时编辑生成内容）
- 进度可视化面板

### 5.2 后端
- Python (FastAPI)
- AI Agent 编排（多轮对话、记忆管理、一致性检查）
- SQLite/PostgreSQL 存储项目数据

### 5.3 AI 集成
- LLM API 调用（模型待确认）
- 长文本上下文管理策略
- Prompt 模板系统（按网文类型 + 阶段动态构建）

---

## 6. 已确认决策

| 决策项 | 选择 |
|-------|------|
| 产品形态 | Web 应用（React + Python FastAPI） |
| 世界观格式 | 结构化模板（提供世界观模板供用户填写各模块） |
| AI 模型方案 | 调用云端 API（主流大模型） |
| 写作流程 | 先生成故事大纲 + 世界观揭示计划 → 用户审核修改 → 逐章生成正文 |

## 7. 待确认事项（剩余）

详见对话中的完整列表。

